{
  "name": "passport-jest-test",
  "version": "0.0.1",
  "description": "minimal repro set for passport memory leak in jest",
  "main": "index.js",
  "engines": {
    "node": "10.4.1",
    "npm": "^5.6.0"
  },
  "scripts": {
    "test": "jest --detectLeaks --config ./jest.config.js"
  },
  "dependencies": {
    "jest": "^23.6.0",
    "passport": "^0.4.0",
    "weak": "^1.0.1"
  }
}
